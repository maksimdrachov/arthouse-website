{% extends "layouts/base.njk" %}

{% block content %}
  <section class="window">
    <div class="window-title">{{ heading }}</div>
    <div class="window-body">
      <h1>{{ heading }}</h1>
      <p>{{ detail }}</p>
      <p><a href="/">Back to scaffold home</a></p>
    </div>
  </section>
{% endblock %}
