{% extends "layouts/base.njk" %}

{% block content %}
  <section class="window">
    <div class="window-title">Edit profile</div>
    <div class="window-body">
      <h1>Edit profile</h1>

      {% if errors.length %}
        <div class="message message-error" role="alert">
          <ul>
            {% for error in errors %}
              <li>{{ error }}</li>
            {% endfor %}
          </ul>
        </div>
      {% endif %}

      {% if artist.bannerPath %}
        <img class="profile-banner" src="{{ artist.bannerPath }}" alt="">
      {% endif %}

      <form class="form-grid" action="{{ formAction }}" method="post" enctype="multipart/form-data">
        <label class="form-row">
          <span>Artist name</span>
          <input name="name" value="{{ values.name }}" autocomplete="name" required>
        </label>

        <label class="form-row">
          <span>New password</span>
          <input name="password" type="password" autocomplete="new-password" minlength="8">
        </label>

        <label class="form-row">
          <span>Bank account</span>
          <input name="bankAccount" value="{{ values.bankAccount }}" autocomplete="off" required>
        </label>

        <label class="form-row">
          <span>About</span>
          <textarea name="about" rows="4">{{ values.about }}</textarea>
        </label>

        <label class="form-row">
          <span>Telegram channel</span>
          <input name="telegram" value="{{ values.telegram }}" autocomplete="off">
        </label>

        <label class="form-row">
          <span>Instagram</span>
          <input name="instagram" value="{{ values.instagram }}" autocomplete="off">
        </label>

        <label class="form-row">
          <span>Store banner (1100 x 250 px)</span>
          <input name="banner" type="file" accept="image/jpeg,image/png,image/gif,image/webp" aria-describedby="banner-dimension-warning" data-banner-upload data-required-width="1100" data-required-height="250">
          <span class="field-warning" id="banner-dimension-warning" role="alert" hidden data-banner-warning></span>
        </label>

        <div class="banner-cropper" hidden data-banner-cropper>
          <div class="banner-cropper-frame">
            <canvas width="1100" height="250" data-banner-crop-canvas></canvas>
          </div>
          <div class="banner-cropper-controls">
            <label class="banner-cropper-zoom">
              <span>Zoom</span>
              <input type="range" min="1" max="4" step="0.01" value="1" data-banner-crop-zoom>
            </label>
            <button type="button" data-banner-crop-apply>Apply crop</button>
          </div>
        </div>

        <div class="form-actions">
          <button type="submit">Save profile</button>
          <a href="{{ cancelUrl }}">Cancel</a>
        </div>
      </form>
    </div>
  </section>
{% endblock %}

{% block scripts %}
  <script src="/scripts/profile-edit.js"></script>
{% endblock %}
